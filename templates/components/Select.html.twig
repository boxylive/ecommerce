<select{{ attributes.defaults({
    class: 'max-w-full rounded-md border border-gray-300 py-1.5 text-left text-base font-medium leading-5 text-gray-700 shadow-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500 sm:text-sm'
}) }}>
    {% block content %}{% endblock %}
</select>
